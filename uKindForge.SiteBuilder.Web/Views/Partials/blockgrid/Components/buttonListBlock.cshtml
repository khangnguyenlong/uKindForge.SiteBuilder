@inherits UmbracoViewPage<BlockGridItem<ButtonListBlock, ButtonBlockSettings>>

@{
    var buttonModel = new ButtonViewModel
    {
        Buttons = Model.Content?.Buttons,
        Align = Model.Settings?.ButtonsAlign
    };
    @await Html.PartialButtonLinkAsync(buttonModel)
}