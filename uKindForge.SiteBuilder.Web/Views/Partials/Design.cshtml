@inherits UmbracoViewPage<DesignDetail>
@using uKindForge.SiteBuilder.Core.Services
@inject IDesignService _designService;

@{
    if (Model == null) return;
    var cssStyle = await _designService.GenerateCssStyle(Model);
}

@Html.Raw(cssStyle?.CssStyle)